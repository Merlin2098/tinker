# Layer 2 Header — immutable_resource_respect
name: immutable_resource_respect
version: "1.0.0"
category: Governance
tier: core
cluster: governance
priority: 97
purpose: "Prevents modification of immutable resources and protected infrastructure"
triggers:
  keywords: [immutable resource, read only, do not modify, protected resource, external dependency]
  match_extensions: []
  match_phases: []
  match_errors: []
requires: []
exclusive_with: []
modes: [ANALYZE_AND_IMPLEMENT]
agent_binding:
  mandatory: [agent_inspector, agent_executor, agent_senior]
  optional: []
body_file: "immutable_resource_respect.md"
token_estimate: 500
execution:
  entrypoint: "agents/tools/wrappers/policy_guidance_wrapper.py::run"
  command: ".venv/Scripts/python.exe agents/tools/run_wrapper.py --skill immutable_resource_respect --args-json '<json-object>'"
  args_schema:
    type: object
    required: []
    properties:
      objective:
        type: string
      target_paths:
        type: array
        items:
          type: string
      constraints:
        type: array
        items:
          type: string
      output_mode:
        type: string
      max_items:
        type: integer

