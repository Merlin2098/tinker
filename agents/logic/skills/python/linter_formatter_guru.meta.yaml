name: linter_formatter_guru
version: 1.0.0
category: Python
tier: lazy
cluster: python_quality
priority: 48
purpose: Pylint, Flake8, Black, Autopep8 with pre-commit hooks
triggers:
  keywords:
  - linter_formatter_guru
  - linter formatter guru
  - linter
  match_extensions:
  - .toml
  match_phases:
  - implement
  match_errors: []
requires: []
exclusive_with: []
modes:
- IMPLEMENT_ONLY
agent_binding:
  mandatory:
  - agent_executor
  - agent_senior
  optional:
  - agent_inspector
max_phase: 2
body_file: linter_formatter_guru.md
token_estimate: 220
execution:
  entrypoint: agents/tools/wrappers/python_quality_advisor_wrapper.py::run
  command: .venv/Scripts/python.exe agents/tools/run_wrapper.py --skill linter_formatter_guru
    --args-json '<json-object>'
  args_schema:
    type: object
    required: []
    properties:
      objective:
        type: string
      target_paths:
        type: array
        items:
          type: string
      constraints:
        type: array
        items:
          type: string
      output_mode:
        type: string
      max_items:
        type: integer

