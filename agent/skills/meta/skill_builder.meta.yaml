name: skill_builder
tier: core
cluster: meta
priority: 95
purpose: "Allows the agent to self-improve by creating and registering new skills."
checks:
  - "User approval required for 'safe_mode' (not implemented yet)"

triggers:
  match_phases:
    - plan
    - improvement
  match_keywords:
    - "create skill"
    - "build tool"
    - "generate wrapper"
    - "implement capability"

# The implementation wrapper
wrapper_path: agent_tools/wrappers/skill_builder_wrapper.py
