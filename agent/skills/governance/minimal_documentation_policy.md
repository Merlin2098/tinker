# Skill: minimal_documentation_policy

The agent restricts documentation generation.

Rules:
- Does not generate documentation by default
- Produces documentation only when explicitly requested
- Keeps documentation minimal and purpose-driven
- Avoids duplication and unnecessary verbosity