# agent.md

Compatibility alias for runtimes that read `agent.md` instead of `AGENTS.md`.
Authoritative file: `AGENTS.md`.

Optional helper for plan review/handoff:
- `python agent_tools/plan_doc.py --help`
- Plan docs are convenience artifacts, not required for execution.
