# ============================================================================
# Tinker Framework Repository — .gitignore
# ============================================================================
# This gitignore is for the framework DEVELOPMENT repository.
# When deploying to a HOST project, the host should add the framework itself
# to their gitignore (see PORTABLE_DEPLOYMENT.md for recommended entries).

# ============================================================================
# Standard Python ignores
# ============================================================================
# Virtual environments
.venv/
venv/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.eggs/

# Testing and tools cache
.cache/
.mypy_cache/
.pytest_cache/

# ============================================================================
# IDE and OS
# ============================================================================
.vscode/
.claude/
.DS_Store
Thumbs.db

# ============================================================================
# Build artifacts
# ============================================================================
build/
dist/
*.spec

# ============================================================================
# Framework-specific runtime files
# ============================================================================
# These are GENERATED files that should never be committed to the framework repo.
# When deployed to a host, these live inside the gitignored agents/logic/ directory.

# Auto-generated analysis files (regenerated on each context load)
agents/logic/analysis/treemap.md
agents/logic/analysis/dependencies_report.md
agents/logic/analysis/dependencies_graph.json
agents/logic/analysis/architecture_metrics.yaml

# Dynamic agent outputs (plans, reports, logs)
agents/logic/agent_outputs/plans/
agents/logic/agent_outputs/reports/
agents/logic/agent_outputs/archive/
agents/logic/agent_logs/

# Message queue runtime files (keep .gitkeep files)
agents/logic/agent_protocol/message_queue/pending/*
agents/logic/agent_protocol/message_queue/in_progress/*
agents/logic/agent_protocol/message_queue/completed/*
!agents/logic/agent_protocol/message_queue/pending/.gitkeep
!agents/logic/agent_protocol/message_queue/in_progress/.gitkeep
!agents/logic/agent_protocol/message_queue/completed/.gitkeep

# Application-specific runtime files
config/json/path_cache.json
logs/
*.log
data/

# ============================================================================
# NOTE: Framework core files (.clinerules, agents/logic/, agents/tools/, etc.)
# ============================================================================
# These files are NOT gitignored in the framework repo because they ARE the
# framework source code. However, when the framework is DEPLOYED to a host
# project, the host should add these to THEIR .gitignore:
#
#   agents/logic/
#   agents/tools/
#   .clinerules
#   agent_framework_config.yaml
#   trigger_vscode.md
#   PORTABLE_DEPLOYMENT.md
#
# See PORTABLE_DEPLOYMENT.md for deployment instructions.



